# 2026-06-16 08:30:13 UTC — Make the component/Pact layer an opt-in choice in the canonical multitier projects

## TL;DR

**Why:** The in-process component + Pact contract tests (plan `20260616-0720`) were added straight into the **default** build of the canonical `backend-java` (`src/test`, deps on `testImplementation`) and `frontend-react` (default `npm test`). Because these projects are **cloned by students and generated by gh-optivem**, that *forces* a heavier, infra-flavoured testing strategy onto existing ATDD students who never opted in — and risks propagating cost/infra (a Pact Broker) into something students inherit by default.

**End result:** The component/Pact layer lives in the **same** canonical projects but **off the default build** — a dedicated Java `componentTest` source set/task and frontend opt-in scripts — so `./gradlew build` and `npm test` are unchanged for existing students. Contract distribution defaults to the free, zero-infra `@PactFolder` + committed pact; a Pact Broker is a documented, cost-labelled opt-in only. The opt-in layer is shaped so gh-optivem can toggle its inclusion at generation time (`--with-contract-tests`), giving a "pristine" project to anyone who doesn't opt in — **without** duplicating projects or pipelines.

## Outcomes

What we get out of this — the goals and deliverables:

- **Default build is byte-for-byte unchanged for existing students.** `./gradlew build` / `./gradlew test` (Java) and `npm test` (frontend) run exactly the tests/deps they did before this layer existed. Nobody is forced onto component/contract tests.
- **The layer is preserved, not reverted** — same tests, same realism (RANDOM_PORT, Testcontainers-Postgres, WireMock, Pact) — just relocated behind an explicit opt-in command (`./gradlew componentTest`, `npm run test:component` / `test:pact`).
- **Zero cost/infra inherited by cloning students on the default path.** Pact contract sharing stays on `@PactFolder` + committed pact ($0, no service). A Pact Broker (PactFlow free = 2 contracts/no `can-i-deploy`; Team ≈ $115/mo; or self-host) is documented as an **opt-in, explicitly cost-labelled** path for multi-repo only — never the default.
- **Single source of truth, no doubled projects.** No duplicate "showcase" repo/pipeline/sonar/VERSION; production code can't drift from a copy.
- **gh-optivem generation seam.** The opt-in source set / scripts are exactly what a future `--with-contract-tests` flag toggles; a student who doesn't opt in never receives the files. No rework needed later.
- **Scope: multitier `backend-java` + `frontend-react` only.** Monolith untouched. Mirroring into `backend-dotnet` / `backend-typescript` remains a documented follow-up.

## ▶ Next executable step (resume here)

**Step 1 is DONE** (committed in `355ebde0`: backend-java restructured into a `componentTest` source set, off the default build). **Resume at Step 2 — frontend opt-in scripts.** In `system/multitier/frontend-react`: make default `npm test` **exclude** `src/test/component/**` + `src/test/pact/**` (vitest config exclude or a second config), and add opt-in `test:component` / `test:pact` scripts; keep Pact/RTL/Vitest in `devDependencies`. This amends the already-committed `99b80815` (a split, not a revert). Gate: default `npm test` no longer runs the component/pact specs; `npm run test:pact` still regenerates the pact. Then Step 3 (mono `shop/contracts/` move — multi-repo half pending the Open question).

## Steps

- [x] Step 1: **Java `componentTest` source set** — DONE (committed in `355ebde0`). Component/contract tests now in `src/componentTest/java`; `build.gradle` has the `componentTest` source set + `componentTestImplementation`/`componentTestRuntimeOnly` configs (WireMock + Pact deps off the default `test` classpath) + a `componentTest` task not wired into the default build. `./gradlew build`/`test` unchanged.
- [ ] Step 2: **Frontend opt-in scripts** — in `system/multitier/frontend-react`, make default `npm test` **exclude** `src/test/component/**` + `src/test/pact/**` (vitest config exclude or separate config); add opt-in `test:component` / `test:pact` scripts. Keep Pact/RTL/Vitest in `devDependencies`. This *amends* the already-committed `99b80815` work (a move/split, not a revert).
- [ ] Step 3: **Contract distribution = free default, neutral `contracts/` location.** Apply the rule *"the contract lives in the nearest repo that contains both consumer and provider."*
  - **Mono-repo:** move the pact out from under the consumer (`frontend-react/pacts/`) into a neutral repo-owned **`shop/contracts/`** folder. Consumer (frontend) writes there; provider points `@PactFolder` at it — no more reaching sideways into the consumer's directory (`../frontend-react/pacts`), which coupled the provider to the consumer's internal layout. $0, no service.
  - **Multi-repo:** the current `gh-optivem` model is **flat sibling repos — no umbrella** (`../shop-backend`, `../shop-frontend`, `../shop-tests`, located by `repolocator`'s sibling-folder convention; see `inferRepos` in `config_commands.go` and `multiRepoMultitierBody` in `config_commands_test.go`). The archived `greeter-multi-repo` umbrella precedent **no longer applies**. So there is no common-ancestor repo; the contract must live in one of the existing siblings or be a published artifact — **design decision, see Open questions.** Default leaning: the **system-test repo (`shop-tests`)**, which already hosts cross-tier contract tests (`ct-test`) and the DSL/driver. Still **no new repo, no broker**.
  - Confirm nothing on the default path requires a broker or network service.
- [ ] Step 4: **Wire opt-in into CI (non-default job).** Add a separate, clearly-optional CI job/stage that runs `componentTest` + frontend `test:pact` (no compose). The existing build jobs stay unchanged. Update `compile-all.sh` only if needed (compile must still pass; these tests are opt-in to *run*).
- [ ] Step 5: **Document the opt-in + broker-as-opt-in.** Project READMEs (and ATDD/architecture docs) get an "Optional: in-process component & contract tests" section: how to run them (`./gradlew componentTest`, `npm run test:component`/`test:pact`), how to remove the layer if unwanted (delete the source set / folder — there is intentionally **no generation flag**, see Decisions), the zero-infra `contracts/` + `@PactFolder` default, and a clearly cost-labelled note that a Pact Broker is a further opt-in for multi-repo (PactFlow caps / ~$115-mo / self-host burden stated plainly). **No gh-optivem changes in this plan.**
- [ ] Step 6: **Mirror into the other multitier backends — DEFERRED.** Tracked as a follow-up in `plans/deferred/20260616-0830-component-pact-layer-other-multitier-backends.md` (mirror the opt-in component/Pact layer into `backend-dotnet` + `backend-typescript` once the java/react shape is proven). Not part of this plan's execution.
- [ ] Step 7: **Reconcile with plan `20260616-0720` (now deleted, merged here).** That plan's backend seam (its Steps 1–7) is **already committed in `355ebde0`** and is the same work as this plan's Step 1. Its remaining Steps 8–9 ("wire into default build", "parity") are superseded by this opt-in structure. Its Step-9 follow-up (the consumer-pact-drift fix) is preserved below and rides along on the relocated frontend tests (Step 2/3).

### Frontend consumer-pact-drift fix (ported from plan 20260616-0720, Step-9 follow-up)

Provider verification surfaced **three** genuine drifts where the consumer pact encodes expectations the real backend (and the Java/.NET/TS **system tests**) do not honour. The backend is correct in all three; the **frontend consumer pact is wrong**. They are currently **skipped** in `componentTest`'s `contract/BackendPactVerificationTest` via the `EXCLUDED_INTERACTIONS` set (JUnit `Assumptions.assumeFalse`). To resolve (rides along with the frontend relocation in Steps 2–3):

1. **publish-coupon** — `coupon.pact.test.tsx`: change `willRespondWith` from `status: 201, body: { code: like('SAVE10') }` to `status: 204` with no body, and stop asserting `result.data.code`. Then `coupon-service.ts` + `types/api.types.ts`: make `createCoupon` return `Result<void>` (drop `CreateCouponResponse`).
2. **error responses** (place-order blackout 422, view-order-details missing 404) — the relevant consumer tests assert `Content-Type: application/json`, but the backend returns RFC-7807 `application/problem+json` for errors. Update those interactions' expected `Content-Type` to `application/problem+json`.
3. Regenerate the pact (`npm run test:pact`) → the committed pact (relocated to `shop/contracts/` per Step 3) carries the corrected interactions.
4. `contract/BackendPactVerificationTest.java` — **empty the `EXCLUDED_INTERACTIONS` set** (and drop the now-unnecessary `@State("no coupon SAVE10 exists yet")` no-op) so all 7 interactions verify.

## Decisions (resolved)

- **Java realism →** full Testcontainers-Postgres harness in the `componentTest` source set; no no-Docker lite variant (Docker is part of the documented opt-in cost).
- **Contract distribution →** free `contracts/` + `@PactFolder` default; broker is a documented, cost-labelled opt-in only. Rule: *contract lives in the nearest repo containing both consumer and provider* → mono = `shop/contracts/`, multi = umbrella root repo's `contracts/`.
- **No generation flag now (YAGNI).** Don't build a gh-optivem `--no-component-testing` toggle. The source-set / folder isolation (Steps 1–2) already keeps the layer **off the default build**, so it's never *forced* — "present but dormant" clears the bar. Generated projects simply always contain the layer; opting out = ignore or delete the folder. This keeps the plan **shop-only** (no gh-optivem changes). Residue accepted: the frontend's `npm install` still pulls Pact/Vitest/RTL devDeps even if unused (Java's separate `componentTestImplementation` resolves only on `./gradlew componentTest`, so its default build is unaffected). A generation exclusion flag is **deferred** — trivially addable later because the layer is already isolated.
- **Other multitier backends →** deferred to `plans/deferred/20260616-0830-component-pact-layer-other-multitier-backends.md`.
- **Generation exclusion flag →** deferred to `plans/deferred/20260616-0830-gh-optivem-component-testing-exclusion-flag.md` (build only if demand appears).

## Open questions

- **Multi-repo contract distribution.** With the current `gh-optivem` flat-sibling model (no umbrella repo), where does the pact live so an independent provider repo can verify it? Options: (a) the **system-test repo (`shop-tests`)** `contracts/` folder — already the cross-tier home of `ct-test` + DSL/driver, reached via the sibling-folder convention locally / fetched in CI; (b) a **published CI artifact** (release asset/package) the provider pulls. Both stay $0/no-broker. **Affects only the multi-repo half of Step 3** — the mono-repo path (`shop/contracts/`) and Steps 1–2 are unblocked.
